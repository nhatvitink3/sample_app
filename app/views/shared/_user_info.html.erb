<%= link_to gravatar_for(current_user, size: Settings.digits.digit_50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".profile"), current_user %></span>
<span>
  <%= pluralize current_user.microposts.count,
  t(".micropost_singular"), plural: t(".micropost_plural") %>
</span>
